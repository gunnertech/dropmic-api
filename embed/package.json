{
  "name": "dropmic-embeds",
  "version": "0.0.0",
  "private": true,
  "engines": {
    "node": "0.12.x"
  },
  "dependencies": {
    "prompt": ">= 0.0.0",
    "autorecordmic": ">= 0.0.0",
    "getmac": ">= 0.0.0",
    "request": ">=0.0.0",
    "gpio": ">=0.0.0",
    "serialport": ">=0.0.0"
  }
}
